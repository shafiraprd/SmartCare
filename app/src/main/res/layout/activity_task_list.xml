<?xml version="1.0" encoding="utf-8"?>
<!-- Layout utama pakai CoordinatorLayout supaya bisa integrasi dengan komponen seperti AppBar dan FloatingActionButton -->
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".TaskListActivity">

    <!-- Bagian atas: AppBar (biasanya untuk toolbar atau judul halaman) -->
    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Toolbar untuk judul halaman -->
        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar_task_list"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="?attr/colorPrimary"
            app:titleTextColor="@android:color/white"/>
    </com.google.android.material.appbar.AppBarLayout>

    <!-- Daftar tugas ditampilkan dalam bentuk list pakai RecyclerView -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_tasks"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="8dp"
        app:layout_behavior="@string/appbar_scrolling_view_behavior" />
    <!-- layout_behavior ini biar RecyclerView bisa scroll dengan mulus saat AppBar ikut geser -->

    <!-- Tombol tambah tugas di pojok kanan bawah -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_add_task"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        android:src="@android:drawable/ic_input_add"
        android:contentDescription="Tambah Tugas Baru" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
